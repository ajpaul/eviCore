{namespace templates_templateHendrikTest}
/**
 * Container Template
 * @param item Item in context
 */
{template .templateHendrikTest}
    <div class="bp-container template-hendrik-test" data-pid="{$item.name}" style="border: 1px solid red">
        <div class="bp-area template-hendrik-test--area">
            {foreach $child in $item.children}
            	{include($child) |noAutoescape}
            {/foreach}
        </div>
    </div>
{/template}
